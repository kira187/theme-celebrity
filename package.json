{
  "name": "src",
  "version": "1.0.0",
  "description": "Theme for landing page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/tailwind.css --watch"
  },
  "author": "Misael AJ",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
  }
}
